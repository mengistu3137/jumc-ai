Frontend

Mobile-first React app scaffold (STEP 2).

Run locally:

1. cd frontend
2. npm install
3. npm run dev

This step implements a simple conversation view and a large microphone button.
Frontend

This folder will contain the mobile-first React app for the voice-enabled assistant.

Structure:
- src/: React source files
- public/: static files (added later)

Design: mobile-first, TailwindCSS, large microphone button, conversation view.

Next: scaffold React app in STEP 2.