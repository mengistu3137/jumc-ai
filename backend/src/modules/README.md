# Domain Modules

Each folder represents a hospital domain capability used by the workflow engine.

- `voice`: input payload lifecycle and validation
- `nlp`: command understanding and parser orchestration
- `workflows`: orchestration logic (prescription, lab, notifications)
- `pharmacy`, `patients`, `laboratory`, `billing`, `doctorNotes`, `notifications`: hospital business domains
