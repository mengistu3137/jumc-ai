Backend

This folder will contain the Node.js + Express API for receiving parsed commands and orchestrating hospital workflows.

Structure:
- src/: server code
- routes/: API routes
- services/: business logic (command interpreter, workflow engine)

Next: implement API endpoints in STEP 6.